
function camShake(_magnitude, _time){
	with(oCam){
		time = _time;
		magnitude = _magnitude;
	}
}

function camZoom(_zoom, _target, _smooth){	
	with(oCam){
		zoom = _zoom;
		target = _target;
		smooth = _smooth;
	}
}